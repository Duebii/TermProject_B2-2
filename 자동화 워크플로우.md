<img width="941" height="492" alt="스크린샷_2026-06-27_오후_8 17 07" src="https://github.com/user-attachments/assets/8f1cc6ee-bf29-4cfb-bba1-a97bde6f52f1" />


**[연결 구조]**

**[1] 스케줄 트리거 (Schedule Trigger)**

- 설정: 매일 18:00 정각 정기 실행 (당일 생성 기사 수집 최적화)
- 타임존: Asia/Seoul
↓

**[2] 멀티 채널 RSS 수집 (RSS Ingestion & Merge)**

- 대상: AI 타임스 / TechCrunch / The AI 피드 파싱
- 수집: 3개 매체의 비동기 피드 데이터를 단일 데이터 스트림으로 결합(Append)
↓

**[3] 데이터 전처리 및 키워드 연관성 필터링 (Code in JavaScript)**

- 정제: HTML 태그 제거 및 공백 정규화 알고리즘 동작
- 분석: 14개 핵심 AI 키워드 기반 포함 빈도 매칭 및 연관성 스코어링(score) 부여
- 1차 중복 제어: 정적 데이터 메모리(Static Data) 공간에 최근 처리된 20개 GUID 상시 저장 및 비교
- 선별 규칙: 중복되지 않은 기사 중 1순위(AI 고득점 최신작) 및 2순위(일반 최신작) 분류 후 최종 1건 선출
- 예외 차단: 수집 대상 풀이 전량 중복 기사일 경우, 빈 배열(return [])을 반환하며 하위 AI API 레이어 트리거를 원천 차단(조기 종료)
↓

**[4] AI 요약 (Message a model - Gemini 2.5 Flash)**

- 프롬프트: 지정된 한국어 번역 가이드라인 및 요약 프롬프트 컨텍스트 적용
- 제약조건: 가독성 극대화를 위해 엄격하게 3줄 요약 제한 생성
↓

**[5] 동적 출처 매핑 및 데이터 송신 (HTTP Request)**

- 출처 식별: URL 도메인을 스캔하는 삼항 연산 인라인 코드를 적용하여 유실될 수 있는 매체명(TechCrunch/ AI타임스 / 디에이아이 / 기타)을 동적 추출
- 웹훅 발행: 가공 완료된 데이터 페이로드(Payload) 패키지를 Make.com 엔드포인트(POST)로 전송
↓

**[6] 실시간 알림 발송 (Make.com - HTTP Request)**

- 알림 전파: 웹훅 수신 즉시 협업 생산성 향상을 위해 Discord 채널 엔드포인트로 AI 뉴스 요약본 실시간 푸시 알림 스트리밍
↓

**[7] 이중 무결성 검색 및 검증 (Make.com - Notion Search Objects)**

- 중복 조회: 분산 환경의 비동기 동시성 이슈를 차단하기 위해, 실제 노션 적재 직전 Target DB 내 동일한 고유 식별자(GUID) 존재 여부 사전 조회
- 조회 최적화: 불필요한 API 비용 방지를 위해 최대 검색 결과 수를 1개(Limit: 1)로 하드 제한 설정
↓

**[8] 이중 필터 및 최종 노션 DB 저장 (Make.com - Notion Create Item)**

- 이중 필터: 앞선 조회 결과 '기존 적재 데이터 없음(Total number of bundles = 0)' 조건 만족 시에만 필터 라우터 통과
- 매핑 적재: Title(제목) / Summary(3줄 요약) / Link(원문 URL) / Date(발행일시) / Source(출처) 속성을 완벽히 매핑하여 최종 인스턴스 영구 적재
↓

**[9] 예외 처리 및 장애 정책 (Error Handling Policy)**

- LLM API 장애 케이스: 제미나이 호출 실패 시 즉시 다운되지 않고 3초 간격으로 최대 3회 자동 재시도(Retry) 수행
- 최종 실패 핸들링: 3회 재시도 모두 실패 시, 불완전 데이터 적재 방지를 위해 워크플로우 즉시 강제 중단(Stop Workflow) 처리 후 로그 기록
